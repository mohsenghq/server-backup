# Progress Tracker

This file is the single source of truth for "where are we." Read it first, every session. Update it last, every session, before ending. See `CLAUDE.md` for the exact protocol.

## Current phase

**Not started.** Phase 0 (Foundations) has not begun.

## Current status

Repository not yet scaffolded. Only the spec (`CLAUDE.md`, `ROADMAP.md`, `docs/`) exists.

## Next action

Start Phase 0 per `ROADMAP.md`: initialize the Rust workspace (`crates/aegis-core`, `aegis-cli`, `aegis-server`, `aegis-agent`, `aegis-mcp`), set up CI, implement chunking + hashing + local backend, get `aegis backup`/`aegis restore` working end-to-end against a local repo.

## Session log

_(newest first — append one short entry per work session; do not delete old entries)_

- No sessions yet.

## Deviations from ROADMAP.md / docs

_(record any substitution of a library/approach from what the docs specify, with a one-line reason)_

- None yet.

## Known issues / open questions

_(anything that needs a second look, or a question for the user before proceeding)_

- None yet.
